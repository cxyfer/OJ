#include <bits/stdc++.h>
using namespace std;
#define endl '\n'

int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
    char a, b, c;
    cin >> a >> b >> c;
    cout << b << c << a << " " << c << a << b << endl;
    return 0;
}