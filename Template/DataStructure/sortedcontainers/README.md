模仿 `sortedcontainers` 的 `SortedList` / `SortedSet` / `SortedDict` 簡單實現。
使用 AI 改寫 `sortedcontainers` 中的原始碼，在保留實現方式和介面的基礎上，簡化程式碼。
未經過充分測試，可能存在錯誤，請謹慎使用。
在平台允許 `sortedcontainers` 的情況下，請優先使用 `sortedcontainers`。